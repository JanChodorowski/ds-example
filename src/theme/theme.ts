import { Theme } from "./types";

import {
  breakpoints,
  colors,
  fontSizes,
  fontWeights,
  lineHeights,
  mediaQueries,
  motion,
  radii,
  space,
} from ".";

export const theme: Theme = {
  breakpoints,
  colors,
  fontSizes,
  fontWeights,
  lineHeights,
  mediaQueries,
  motion,
  radii,
  space,
};
